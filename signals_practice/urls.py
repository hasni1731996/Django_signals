from django.conf.urls import url
from .views import *

urlpatterns = [

    # url(r'^cookbook/$', recipes_view),  # Getting records from recipes model through this url
    #
    # url(r'^show_time_temp/$', ShowTimeView.as_view()),
    # Getting current date time through HTML template & Message displayed as well

]